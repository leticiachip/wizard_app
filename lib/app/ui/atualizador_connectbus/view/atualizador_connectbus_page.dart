import 'package:flutter/material.dart';

class AtualizadorConnectbusPage extends StatelessWidget {
  const AtualizadorConnectbusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Atualizador connect bus")));
  }
}
